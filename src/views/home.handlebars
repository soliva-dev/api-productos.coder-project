<style>
    .info-text {
        color: #666;
        font-size: 0.9rem;
    }
    .products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 1.2rem;
        margin-top: 1rem;
    }
    .product-description {
        color: #555;
        margin: 0.5rem 0 0.8rem;
        font-size: 0.95rem;
        line-height: 1.4;
    }
    .product-details {
        display: flex;
        flex-wrap: wrap;
        gap: 0.6rem;
        font-size: 0.9rem;
        color: #444;
    }
    .product-content {
        padding: 15px 18px;
        font-size: 0.93rem;
        line-height: 1.5;
        color: #444;
    }
    body {
        font-family: "Segoe UI", Arial, sans-serif;
        background-color: #f4f6fa;
        color: #333;
        margin: 0;
        padding: 40px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    h2 {
        color: #1f3a6f;
        margin-bottom: 20px;
        text-align: center;
    }
    .product-card {
        background-color: #ffffff;
        border: 1px solid #e3e7ef;
        border-radius: 10px;
        padding: 0;
        box-shadow: 0 3px 8px rgba(0, 0, 0, 0.05);
        transition: transform 0.15s ease, box-shadow 0.15s ease;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }
    .product-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
    }
    .product-header {
        background-color: #f5f8ff;
        padding: 12px 16px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 1px solid #e0e6f1;
    }
    .product-title {
        font-weight: bold;
        font-size: 1rem;
        color: #1f3a6f;
        margin: 0;
    }
    .product-content {
        padding: 15px 18px;
        font-size: 0.93rem;
        line-height: 1.5;
        color: #444;
    }
    .product-id {
        font-size: 0.85rem;
        color: #777;
        margin-bottom: 10px;
    }
    .product-info span {
        display: block;
        margin-bottom: 4px;
    }
    .product-info b {
        color: #111;
    }
    .no-products {
        grid-column: span 2;
        text-align: center;
        color: #777;
        font-style: italic;
        background: #fafafa;
        border: 1px dashed #ccc;
        border-radius: 8px;
        padding: 20px;
    }
</style>

<h2>Productos listados (solo lectura)</h2>
<p class="info-text">Esta lista se actualiza solo si recargas la pagina despues de un cambio en la API.</p>

<div class="products-grid">
    {{#each products}}
        <li class="product-card" id="product-{{this.id}}">
            <div class="product-content">
                <div class="product-header"><h3 class="product-title">{{this.title}}</h3></div>
                <hr>
                <div class="product-id">ID: {{this.id}}</div>
                <div class="product-info">
                    <span><b>Descripcion:</b> {{this.description}}</span>
                    <span><b>Precio:</b> ${{this.price}}</span>
                    <span><b>Stock:</b> {{this.stock}}</span>
                    <span><b>Codigo:</b> {{this.code}}</span>
                    <span><b>Categoria:</b> {{this.category}}</span>
                </div>
            </div>
        </li>
    {{else}}
        <li class="no-products">No hay productos para mostrar.</li>
    {{/each}}
</div>

